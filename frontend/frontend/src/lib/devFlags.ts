/**
 * Kurz: App ohne Login-Gate (Frontend) + Backend braucht `MOCKUP_AUTH_DISABLED=1` in `backend/.env`.
 * Wieder schließen: hier `false`, in `.env` env entfernen/false, und in `App.tsx` den
 * auskommentierten Redirect wieder aktivieren (eine Zeile).
 */
export const MOCKUP_AUTH_DISABLED = true;
